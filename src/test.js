const { M } = require('vite/dist/node/types.d-AKzkD8vd')

var groupAnagrams = function (strsArr) {
  let m = new Map()
  strsArr.forEach((str) => {
    if (m.has(str)) {
      m.get(str)
    }
  })
}

const s1 = 'tea'
const s2 = 'ate'
let m = new Map()
map.set('a', {
  count: 1,
})
console.log(map)
